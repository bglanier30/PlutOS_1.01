#include <limits.h>
#include <stdbool.h>
#include <stdarg.h>
#include <stdio.h>
#include <string.h>
#include <kernel/tty.h>

//TODO: implement a better version of this 
char* sprintf(const char* restrict str1, char * str2) {
	//char outstring[strlen(str1)+strlen(str2)-1];
	
	//memcpy(outstring, str2, strlen(str2));

	return str2;
}
